# 00001 [k3v9d2] PR1 — restore buildability

Branch `fix/restore-buildability`. Plan: `docs/release-plan.md` §5/PR1, rulings R1–R5, R9, R11.

**Goal:** `dotnet build Code/Iql.sln` (Debug+Release) clean and `dotnet test` green (Iql.Tests, Iql.Tests.DotNet, Iql.Tests.Server) on a machine with only the .NET 10 SDK + public nuget.org.

Commit-ordered steps:
1. **Feeds (R1):** rewrite `Code/NuGet.config`: `<clear/>`, nuget.org, local folder source `nuget-local` (path relative to config). Delete the `%BrandlessNuGet%` source.
2. **Vendor TypeSharp.Extensions (R2):** temp checkout `D:\Servers\Cmd\Storage\clones\TypeSharp` @ `dc67c8b` (use a scratch worktree — do NOT touch the TypeSharp repo/worktrees themselves), add `netstandard2.0` to `Code/Core/TypeSharp.Extensions` TargetFrameworks (its Build.props sets them), `dotnet pack -c Debug` with version props so the output is exactly `TypeSharp.Extensions.0.0.6-preview0128-1-Debug.nupkg` (version.xml VersionPrefix=0.0.6, VersionSuffix=preview0128, BuildNumber=1-Debug → suffix composes as preview0128-1-Debug), copy into `nuget-local/`, write `nuget-local/README.md` with the exact reproduction recipe + sha, discard scratch. Verify `dotnet restore Code/Core/Iql/Iql.csproj` resolves it.
3. **Vestigial refs (R3a):** remove `Brandless.ObjectSerializer` + `Brandless.Data.EntityFramework` PackageReferences from `Iql.Tests.csproj` and `Iql.Tests.DotNet.csproj`; delete the dead `using Brandless.ObjectSerializer;` in `Tests/OData/ODataUriTests.cs` + the commented CSharpObjectSerializer lines (ODataUriTests.cs:198,500; JavaScriptExpressionTests.cs:6).
4. **Signing (R4):** delete SignAssembly/AssemblyOriginatorKeyFile/PublicSign from `Code/Build/Build.props`.
5. **TFMs + bumps (R5):** `Code/Core/Build/Build.props` → `netstandard2.0;net10.0`; `Code/Tests/Build/Build.props`, `Code/Tests/Server/Build/Build.props`, `Code/Extensions/Build/Build.props`, `Code/Apps/Build/Build.props` → `net10.0`. Delete: net46/net471 conditional ItemGroups (Iql.DotNet System.Net.Http), netcoreapp3.1 HintPath ItemGroup (`Iql.Server.OData.Net.csproj:9-18`), netcoreapp2.1 block (`Iql.Server.Azure.csproj:12-14`), the 13 absolute-path `<Content Update>` entries (`Iql.Server.ConsoleApp.csproj:17-52` — whole project dies in PR3, but the sln must build now: simplest is to strip the csproj to a bare stub now). Fix `Iql.OData.csproj:6` DefineConstants (remove ABC/TS/dupes; canonical: `TRACE;TypeScript` for TypeScript Debug config + NETSTANDARD2_0 only where the transpile config needs it). Bump: EF Core→10.x, AspNetCore.OData→9.x latest, Identity.Stores→10.x, MSTest→3.x + adapter, Test.Sdk→current, Newtonsoft→13.0.4, Azure.Storage.Blobs→current, CodeAnalysis→current, `Microsoft.AspNetCore` 2.2.0 ref in Iql.Tests.Server → delete (use FrameworkReference Microsoft.AspNetCore.App if needed). Remove DotMemoryUnit (R17) — check for API usage in test bodies first; rewrite/remove affected asserts.
6. **Brandless excision (R3b/c):** minimal-port surface per `docs/release-plan.md` §2.1: `CrudManager` (EntityKey/FindQuery/KeyEqualsExpression/DbSet/Context), `CrudBase<TSec,TUnsec,T>` (Secured/Unsecured/NewUnsecuredDb), Mptt types used by `IqlODataController.cs`, OData EDM binding helpers used by `ServerTestsBase.cs` (`Brandless.AspNetCore.OData.Extensions[.Binding]`), ObjectSerializer usage in ContextGenerator (`DataContextGenerator.cs`, `TypeDetailTypeConverter.cs` — 1 hit each; likely replaceable with Newtonsoft). Port into `Internal/` namespaces with file-header attribution `// Ported from joshcomley/<repo> @ <sha>`. >15-file drag → full-source vendor under `Code/Vendor/<Name>/`. **Q2 answered 2026-07-07: Josh approved porting freely from the private repos** — no gate; keep the attribution headers.
7. **Compiler fallout:** netstandard2.0 LangVersion (set explicitly, e.g. `latest`, in core props; add compat packages/`#if NETSTANDARD2_0` shims only where an API is genuinely missing), MSTest 3 assert deltas, OData 9 routing/EDM deltas in IqlODataController + IqlSampleApp Startup.
8. **SQL for server tests (R11):** parameterize `ApplicationDbContext.ConnectionString` via `IQL_TESTS_SQL_CONNECTION` env (fallback = current literal). Hub has NO SQL Server: install SQL Express LocalDB via the admin gate (HEARTBEAT check → inbox script; fallback `request-admin-action`). Run server tests against `(localdb)\MSSQLLocalDB`.

**Acceptance:** plan §5/PR1 gate. Behavior preservation R9 — no test-expectation changes. Keep the `TypeScript Debug`/`Debug TypeScript CSharp` solution configs evaluating; record what TypeScript Debug currently does (it's allowed to be broken pre-existing — document, don't fix here).

**Gotchas:** worktree-guard hook denies edits to primary checkouts under D:\Servers — the TypeSharp scratch checkout must live in the scratchpad/temp dir, not in the clones tree. `gh` must run via PowerShell (full path), not Bash. Run `git fetch origin && git merge origin/master --no-edit` before starting (handover-receiver rule).
